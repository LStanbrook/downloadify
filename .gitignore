# Python
__pycache__/
*.pyc
.venv/
.venv_build/
venv/
env/
*.egg-info/

# PyInstaller build output -- rebuild with build_exe.ps1 rather than
# committing the ~100MB binary; see that script for the exact command.
build/
dist/
*.spec

# Environment secrets
.env
.spotify_token_cache.json

# Downloaded content -- the app creates this folder itself on first run
downloadify_downloads/

# Editors / OS
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
.idea/
.DS_Store
Thumbs.db
